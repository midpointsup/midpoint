<template>
  <div>Trip info</div>
</template>

<script>
import io from "socket.io-client";

export default {
  mounted() {/*
    const socket = io("http://localhost:3000");

    socket.on("connect", () => {
      console.log("connected");
    });

    socket.on("trip", (trip) => {
      console.log("trip");
      console.log(trip);
    });*/
  },
};
</script>

<style></style>
